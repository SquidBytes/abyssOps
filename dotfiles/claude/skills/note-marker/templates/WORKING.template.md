# Working Notes

<!--
  RULES — how to write in this file so /marknotes parses it correctly:

  1. Each "item" is one paragraph OR one top-level bullet (numbered or -).
     Blank lines separate items.
  2. Nested bullets and wrapped lines stay attached to the parent item.
  3. Use `---` on its own line to break between dates, phases, or topics.
     Do NOT use `---- some label` — put the label on a heading instead.
  4. Use `## <heading>` for date or section markers (e.g. `## 11 April 2026`
     or `## After Phase 25`). The agent uses headings as classification
     context.
  5. Don't write the STATUS line yourself — the agent adds it.
  6. Anything inside an HTML comment block is skipped by the parser, so
     you can leave yourself non-reviewable notes that way.

  When you're done dumping thoughts:
    - `/marknotes`           — classify everything unreviewed
    - `/marknotes --archive` — classify, then roll over into a dated archive
-->
